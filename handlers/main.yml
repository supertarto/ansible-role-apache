---
- name: Start apache on boot
  ansible.builtin.service:
    name: "{{ apache_service }}"
    state: "started"
    enabled: true

- name: Restart apache
  ansible.builtin.service:
    name: "{{ apache_service }}"
    state: restarted
